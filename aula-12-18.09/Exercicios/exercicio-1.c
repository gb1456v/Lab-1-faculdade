#include <stdio.h>

int main()
{
    int num, primo = 0, cont = 0, n = 1;
    printf("\n\n -- Verificador de números primos -- \n\n");
    printf("Digite um número inteiro:\n");
    scanf("%d", &num);
    while (num != 0)
    {
        while (num > 0)
    }
    if (primo >= 2)
    {
        printf(" Você digitou %d numeros primos", cont);
    }
}
